import Ember from 'ember';

export function averageRating(params) {
  // console.log(params);
  // var sum = 0;
  //
  //
  // for( var i = 0; i < review.ratings.length; i++ ){
  //     sum += parseInt( ratings[i]);
  //
  // }
  // var avg = sum/review.ratings.length;
  // return avg;
}
export default Ember.Helper.helper(averageRating);
