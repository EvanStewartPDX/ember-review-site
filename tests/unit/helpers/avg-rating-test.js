
import { avgRating } from 'restaurante/helpers/avg-rating';
import { module, test } from 'qunit';

module('Unit | Helper | avg rating');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = avgRating([42]);
  assert.ok(result);
});

