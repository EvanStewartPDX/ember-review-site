
import { reviewcount } from 'restaurante/helpers/reviewcount';
import { module, test } from 'qunit';

module('Unit | Helper | reviewcount');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = reviewcount([42]);
  assert.ok(result);
});

